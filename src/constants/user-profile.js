const USER_PROFILE = Object.freeze({
    USERNAME: "username",
    EMAIL: "email",
});

const USER_PROFILE_LIST = Object.freeze(Object.keys(USER_PROFILE));

export { USER_PROFILE, USER_PROFILE_LIST };
